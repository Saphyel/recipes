version: "3"

services:
  webapp:
    build: .
    ports: [ "8081:80" ]
    volumes:
      - ./:/app/
    command: pdm run server
    env_file: [ .env ]

  db:
    image: postgres:14
    ports: [ "5432:5432" ]
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: password
    links: [ webapp ]

volumes:
  pgdata:
